name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Link Checker
      uses: gecko655/link-checker@patch-1
      with:
        args: -v -r *

